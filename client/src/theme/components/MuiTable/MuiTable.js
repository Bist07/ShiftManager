const MuiTable = {
    styleOverrides: {
        root: ({ theme }) => ({
            maxHeight: '800px',
            overflow: 'auto',
            borderRadius: '40px',
        }),
    },
};

export default MuiTable;
